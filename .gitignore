node_modules
.next
.next-*
.next-corrupt-cache*
out
.env.local
.env*.local
.DS_Store
tsconfig.tsbuildinfo

# Local demo videos — never publish (3.4GB; GitHub rejects >100MB anyway)
public/NivaDesk_Demo_high.mov
public/NivaDesk_Demo.mp4

# Never again: a nested copy of the source tree here is served by nothing,
# breaks the build with its stale files, and re-enters the index on every
# `git add -A`. The site is served from THIS directory.
studioflow-web/
