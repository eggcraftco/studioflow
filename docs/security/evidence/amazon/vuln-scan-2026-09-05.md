# Dependency vulnerability scan — 2026-09-05 23:38 UTC (first record under the vulnerability-management plan)

Method: `npm audit --json` (npm registry advisory database) run read-only in every
package tree that ships to production, from the checked-out branch at commit
`3a981228`. Nothing was changed. Counts are for production dependencies
(`--omit=dev`); the "incl. dev" column adds development-only packages.
Remediation deadlines follow `docs/security/vulnerability-management.md`
(DPP 2.7.3: critical 7 days, high 30 days; our own moderate 90 days).

| Tree | What it is | Critical | High | Moderate | Low | Incl. dev | Fix available |
|---|---|---|---|---|---|---|---|
| `functions/` | main project Cloud Functions (`eggcraft-studio`) | 1 | 6 | 12 | 1 | 20 | yes — all; 3 need a major bump (`firebase-admin` 14.3.0, `geoip-lite` 1.2.2) |
| `functions-amazon/` | Amazon zone services (`nivadesk-amazon`, image `nivadesk-amazon:c0df21d0`) | 0 | 0 | 8 | 0 | 8 | yes — transitive `uuid`/`teeny-request`/`retry-request`; `firebase-admin` 14.3.0 is the clean path |
| `studioflow-web/` | web app (Next.js) | 1 | 6 | 0 | 0 | 9 | yes — including `next` itself |
| `nivadesk-order-management/`, `nivadesk-web-deploy-temp/` | legacy trees, not deployed (last change 2026-08-28 / untracked) | — | — | — | — | — | out of scope; delete or archive at the next clean-up |

## Findings that carry a deadline

| Severity | Package (transitive unless noted) | Trees | Advisory | Due by |
|---|---|---|---|---|
| critical | `websocket-driver` ≤ 0.7.4 | functions, studioflow-web | resource-limit bypass via message compression; message corruption via protocol length header | **2026-09-12** |
| high | `@grpc/grpc-js` 1.14.0–1.14.3 | functions, studioflow-web | malformed request / compressed message crashes a server or client | 2026-10-05 |
| high | `brace-expansion` | functions | exponential-time expansion (DoS) | 2026-10-05 |
| high | `fast-xml-builder` ≤ 1.1.6 | functions | attribute-quote and comment-regex bypass | 2026-10-05 |
| high | `form-data` < 4.0.6 | functions | CRLF injection in multipart names | 2026-10-05 |
| high | `ip-address` ≤ 10.3.0 (via direct dep `geoip-lite`) | functions | XSS in Address6 HTML methods; leading-zero octet decoding | 2026-10-05 (needs `geoip-lite` 1.2.2, a major change) |
| high | `protobufjs` ≤ 7.6.4 | functions, studioflow-web | code injection through bytes-field defaults; DoS from crafted field names | 2026-10-05 |
| high | `next` (direct) | studioflow-web | denial of service with Server Components | 2026-10-05 |
| high | `nanoid`, `postcss`, `sharp` | studioflow-web | non-secure generator loop; XSS via unescaped `</style>`; inherited libvips CVEs | 2026-10-05 |
| moderate | `uuid` < 11.1.1 and the Google client chain (`google-gax`, `teeny-request`, `retry-request`, `@google-cloud/*`, `firebase-admin`) | functions, functions-amazon | buffer bounds check in uuid v3/v5/v6; overlong UTF-8 in protobufjs; qs/body-parser DoS | 2026-12-04 |
| low | `@tootallnate/once` | functions | incorrect control-flow scoping | 2026-12-04 |

Exposure notes for the Amazon zone: none of its eight findings is reachable
from the network — the zone speaks to Amazon and Google APIs only, accepts
requests only through the load balancer / Cloud Armor, and does not parse
untrusted multipart, XML or WebSocket input. They are still fixed within the
moderate window because the plan says so, not because they are exploitable.

Next scheduled scan: **2026-10-05** (30 days), or earlier after any dependency
change that ships. Container-image scanning for the zone's image is a planned
addition (Artifact Analysis is not enabled on `nivadesk-amazon`; enabling it is
a project change and waits for the freeze to lift).

## Remediation run — 2026-09-05 23:45–23:58 UTC (semver-compatible fixes only, no `--force`, no major upgrades)

| Tree | Command | Result | Remaining after re-audit |
|---|---|---|---|
| `functions/` | `npm audit fix` | 19 packages moved within their existing ranges, 1 added (`xml-naming`, a new dependency of `fast-xml-builder` 1.3.1), 2 dropped; `package.json` unchanged | **0 critical, 1 high** (`ip-address` ≤ 10.3.0 via direct dependency `geoip-lite` 1.4.10 — only fixable by a major change, see below), 11 moderate (the Google client chain pinned by `firebase-admin` 12.x; `qs`, `gaxios`, `body-parser` moderates that `npm audit fix` left in place) |
| `functions-amazon/` | `npm audit fix` | **no change** — the 8 moderates all sit behind `firebase-admin` 13.4's pinned Google client chain; nothing semver-compatible to apply; lockfile untouched | 0 critical, 0 high, 8 moderate |
| `studioflow-web/` | `npm audit fix` | 46 packages moved within their ranges (`next` 15.5.15 → 15.5.25, `sharp` 0.34.5 → 0.35.4 with its platform binaries, `js-yaml` 4.1.1 → 4.3.2, `nanoid` 3.3.12 → 3.3.18, `@grpc/grpc-js` 1.9.15 → 1.9.16, `websocket-driver` 0.7.4 → 0.7.5, `protobufjs` 7.5.6 → 7.6.6, `brace-expansion`, `semver`); `package.json` unchanged | **0 critical, 1 high** (`postcss` 8.4.31, pinned by `next` 15 itself — only `next` 16 changes it), 1 moderate (`next` depending on that `postcss`) |

### Closed by this run

| Severity | Package | Advisories | Trees |
|---|---|---|---|
| critical | `websocket-driver` 0.7.4 → 0.7.5 | GHSA-mp7j-qc5w-4988, GHSA-xv26-6w52-cph6 | functions, studioflow-web |
| high | `@grpc/grpc-js` 1.14.3 → 1.14.4 / 1.9.15 → 1.9.16 | GHSA-5375-pq7m-f5r2, GHSA-99f4-grh7-6pcq | functions, studioflow-web |
| high | `brace-expansion` → 1.1.18 / 2.1.4 / 5.0.9 | GHSA-3jxr-9vmj-r5cp, GHSA-mh99-v99m-4gvg, GHSA-jxxr-4gwj-5jf2 | functions, studioflow-web |
| high | `fast-xml-builder` 1.1.5 → 1.3.1 | GHSA-5wm8-gmm8-39j9, GHSA-45c6-75p6-83cc | functions |
| high | `form-data` 2.5.5 → 2.5.6, 4.0.5 → 4.0.6 | GHSA-hmw2-7cc7-3qxx | functions |
| high | `protobufjs` 7.5.5/7.5.6 → 7.6.6 | GHSA-66ff-xgx4-vchm, GHSA-2pr8-phx7-x9h3, GHSA-fx83-v9x8-x52w, GHSA-jggg-4jg4-v7c6, GHSA-wcpc-wj8m-hjx6, GHSA-f38q-mgvj-vph7 | functions, studioflow-web |
| high | `js-yaml` 4.1.1 → 4.3.2 | GHSA-h67p-54hq-rp68, GHSA-52cp-r559-cp3m, GHSA-5p4m-2wfm-xmqj | studioflow-web |
| high | `nanoid` 3.3.12 → 3.3.18 | GHSA-28wg-ghj8-5hjv, GHSA-2v37-7h3g-55p8 | studioflow-web |
| high | `next` 15.5.15 → 15.5.25 | GHSA-8h8q-6873-q5fj, GHSA-26hh-7cqf-hhc6, GHSA-3g8h-86w9-wvmq | studioflow-web |
| high | `sharp` 0.34.5 → 0.35.4 | GHSA-f88m-g3jw-g9cj | studioflow-web |
| moderate / low | `@protobufjs/utf8` 1.1.0 → 1.1.2, `body-parser` 1.20.5 → 1.20.6, `@tootallnate/once` 2.0.0 → 2.0.1 | GHSA-q6x5-8v7m-xcrf, GHSA-v422-hmwv-36x6, GHSA-vpq2-c234-7xj6 | functions |

### Still open (need a major change — separate decision)

| Severity | Package | Trees | Why it stays | Recommended change | Risk of the change |
|---|---|---|---|---|---|
| high | `ip-address` 5.9.4 (GHSA-v2v4-37r5-5v8g XSS in `Address6` HTML helpers; GHSA-mwp4-54f8-5fhr leading-zero octet decoding) | functions | `geoip-lite` 1.4.10 pins `ip-address 5.8.9–5.9.4`; the fixed line is 10.3.1+, reachable only through `geoip-lite` 2.0.3 (major) | upgrade `geoip-lite` ^1.4.10 → ^2.0.3 (resolves `ip-address` ^10.2.0 → 10.7.0) | low: the code calls only `geoip.lookup(ip)` (one site in `functions/index.js`, lazily loaded); 2.x keeps `lookup()`; verify with the unit suite and one manual lookup. Exposure today is low — the vulnerable helpers are HTML-emitting methods NivaDesk never calls, and the parsing quirk only affects the geo result |
| high | `postcss` 8.4.31 (four advisories incl. GHSA-r28c-9q8g-f849 source-map path traversal) | studioflow-web | pinned inside `next` 15; fixed only by `next` 16.3.4 | plan a `next` 15 → 16 upgrade (major: build/runtime changes, React 19 already in place) | medium: framework major; needs a full web QA pass. Exposure today is build-time only — PostCSS processes NivaDesk's own CSS during the Hostinger build, never untrusted input |
| moderate ×11 | Google client chain (`uuid` GHSA-w5hq-g745-h8pq, `google-gax`, `teeny-request`, `retry-request`, `@google-cloud/firestore`, `@google-cloud/storage`), `qs` (3 advisories), `gaxios`, `geoip-lite` | functions | pinned by `firebase-admin` ^12.7.0 | upgrade `firebase-admin` 12 → 14.3.0 (major) | medium: admin SDK major; touches every function; requires the unit + rules + e2e suites and a staged deploy |
| moderate ×8 | same Google client chain | functions-amazon | pinned by `firebase-admin` ^13.4.0 | upgrade `firebase-admin` 13 → 14.3.0 (major) after the freeze; rebuild the zone image | low–medium: the zone uses Firestore and Secret Manager only; its 9 test files cover the paths |

Deadline check against the plan: the critical (`websocket-driver`) is closed within
the 7-day window in the repository; it reaches production with the next deploy of
the main functions and the web app, which needs the operator's approval. The one
high still open in `functions` (`ip-address`) is due by 2026-10-05 and has a
recommended fix above.

### Production status of the fixes (wording rule: nothing is "remediated in production" until verified there)

| Tree | Status as of 2026-09-06 00:45 UTC |
|---|---|
| `studioflow-web/` | **Remediated in production** — Round 165 (publish repo commit `77e189a`) built by Hostinger at 00:43 UTC and verified: new build serving, six smoke checks passed, recorded in `web-deploy-round-165-2026-09-06.md`. Open on the web: `postcss` (needs next 16) |
| `functions/` | **Partly remediated in production** (2026-09-06 01:38–03:21 UTC): batches B0–B4.5 — 228 of 414 functions (canary, every ingress webhook/OAuth/bridge/MCP surface, all scheduled and event-triggered functions, and every callable last deployed 1–5 September) run on the remediated tree, each batch with rollback targets, probe parity and a clean fifteen-minute gate — record in `functions-deploy-2026-09-06.md`. The remaining 186 (callables last deployed in August, June and July): **remediation committed and tested; production deployment pending** the operator's go per batch (`../../functions-dependency-deploy-plan-2026-09-06.md`) |
| `functions-amazon/` | No change possible without a major (`firebase-admin` 14); nothing to deploy |

### Test results for the remediation (2026-09-06 00:02–00:12 UTC)

| Tree | Suite | Result |
|---|---|---|
| `functions/` | `npm test` (unit, fake Firestore) | pass, exit 0 |
| `functions/` | `npm run test:rules` (Firestore + Storage emulators) | pass — 6 rules suites green, including the storage-scan rules |
| `functions/` | `bash test/run-e2e.sh` (Firestore + Storage emulators) | 143 pass / 12 fail — **the same 12 failures reproduce on a throwaway export of HEAD with the committed (pre-fix) lockfile** (140 pass / 15 fail there; the 3 extra were the Shopify token suite, see below). The dependency change adds no failure |
| `functions-amazon/` | `npm test` | pass, exit 0 |
| `studioflow-web/` | `npm run typecheck`, `npm run test:finance`, `npm run build:next` | pass (no type errors; finance vectors green; production build completes). `npm run lint` is `next lint`, which prompts to set ESLint up and is not a runnable check in this repository |

Two findings about the test tier itself, fixed in the same change set (test and CI files only, no product code):

1. `functions/test/e2e/shopify-token-emulator.test.js` still asserted the Phase A
   dual-write (plaintext beside the box). Commit 4c2116c9 (2026-09-04) switched the
   code to Phase B — box only, plaintext cleared after read-back — with a unit test
   for the new contract but without updating the e2e test. The three assertions
   now state the Phase B contract; the suite is green.
2. `.github/workflows/functions-tests.yml` started the emulator with
   `--only firestore`; `test/qa/storage-scan-rules.test.mjs` (added 2026-09-04 with
   the upload scanner) needs the Storage emulator, so the rules + e2e job has been
   red since 2026-09-05 21:27 UTC (run 33993020490, `ECONNREFUSED 127.0.0.1:9199`)
   before reaching any e2e suite. The job now starts `firestore,storage`.

Pre-existing e2e failures, not touched by this change and outside its scope (a
separate fix; all trace to 2026-09-04 code commits that landed while the CI job was
already red):

| Suite | Failures | Cause |
|---|---|---|
| `inbound-capacity-emulator`, `legacy-paths-retired-emulator` | 5 | `inboundOrderWebhook` reads `req.get("idempotency-key")` since ed8165c9; the tests' fake request object has no `get` → 500 |
| `bank-sync-first-imported-emulator`, `bank-paypal-feed-emulator` | 5 | the server-side plan gate from 985240ca ("Bank and payment feeds are part of NivaDesk Pro") refuses the test workspaces, which carry no paid plan; the other three failures in those files follow from the refused sync |
| `finance-stamp-emulator` | 2 | figures changed with engine version 4 (3bf449b6); the test's expected numbers date from 2026-09-03 |
