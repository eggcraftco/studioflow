# Tracking webhook token — exposure, rotation, revocation

6 September 2026. **The value appears nowhere in this file, and must never be added to it.** Read the
current value with `gcloud secrets versions access latest --secret=TRACK17_WEBHOOK_TOKEN` when you
need it; do not paste it anywhere that is written down.

## Timeline

| When (UTC) | What |
|---|---|
| before today | `TRACK17_WEBHOOK_TOKEN` lived as a plain environment value on the `track17webhook` service, and the handler also accepted it as a `?token=` **query parameter** |
| 2026-09-06 ~17:35 | **Exposure.** An unfiltered `gcloud run services describe` printed every environment entry with its value into this session's transcript, the token among them |
| 2026-09-06 ~17:40 | **Second exposure found, older and wider.** Request logs show the token arriving as a URL query parameter, so it has been written into Cloud Run's `httpRequest.requestUrl` for every delivery that used that form. Of the last three requests sampled, one carried it in the URL |
| 2026-09-06 19:03 | **New value generated and stored** in Secret Manager as `TRACK17_WEBHOOK_TOKEN`, version 1, 64 hex characters from `openssl rand`. Generated by a pipeline that never printed it: `openssl rand -hex 32 \| gcloud secrets create --data-file=-` |
| 2026-09-06 19:0x | Access granted to the function's runtime identity (`477037475099-compute@`), that secret only |
| 2026-09-06 19:0x | **Code changed** (not yet deployed): the handler reads the Secret Manager parameter instead of `process.env`, the function binds it with `secrets: [...]`, and a token arriving in the URL is now **refused** with `token_in_url` before it is compared. The plain value was removed from `functions/.env` |
| pending | **Deploy** of `functions:track17Webhook` — blocked: the Firebase CLI credential has expired |
| pending | **17TRACK reconfigured** to send the new value in the `x-studioflow-token` header — operator only |
| pending | **Revocation proof** — a synthetic request carrying the old value must be refused |

## What the old token could do, and what it could not

It authenticated inbound carrier-tracking callbacks. Someone holding it could post forged tracking
updates for orders whose tracking numbers they knew, which can flip an order's dispatched or delivered
flags and send the customer a status message. It reads no customer data, authenticates to nothing
else, and is not a Google, Stripe or Apple credential.

## What is still true right now

**The rotation is not complete and the old token still works**, because nothing has been deployed. The
new secret exists and the code is written; production is unchanged until the deploy runs.

## What the operator does, in order

1. `firebase login --reauth`
2. Tell the assistant to deploy `functions:track17Webhook` (it will not deploy without being asked).
3. Read the new value once, from your own terminal, and put it into 17TRACK's webhook configuration
   **as a header**, `x-studioflow-token`. The assistant does not enter credentials into third-party
   forms. If 17TRACK cannot send a custom header, say so — the URL form is now refused, and we need a
   different scheme rather than a token in a log.
4. The assistant then proves the old value is refused and the new one is accepted, and records the
   result here.

Between step 2 and step 3 the tracking callbacks will be refused. They retry, and the tracking sweep
re-reads from the carrier, so the cost is delay rather than lost updates.

## Why this is separate from the dependency soak

This is a **production change made during the soak window**, deliberately and with the operator's
instruction. The soak's clean result covers the 350 functions deployed in batches B0–B5.3 and the
absence of errors in that window; it must not be read as "nothing was changed in production on
6 September". When the deploy runs, `track17Webhook` gets a new revision for a security reason that
has nothing to do with the dependency remediation, and the soak record links here rather than
absorbing it.
