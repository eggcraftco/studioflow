# Gece çalışma raporu — 10 Eylül 2026 (operatör uyurken, yetkili kapsam)

Yetki: 10 Eyl gece mesajı (6 iş, sırayla; rutin uygulama/test/commit/push ve açıkça yetkilendirilen deploy'lar için
yeniden onay yok; giriş/MFA/dış yanıt/ürün kararı → BLOCKED; yeni HIGH → o hattın deploy'u durur; force-push/reset yok).
Bu dosya ilerledikçe güncellenir; her deploy için kaynak/revision/rollback kaydı ve "hazır ama yayınlanmamış" ayrımı tutulur.

| İş | Tamamlanan | Commit/branch | Test kanıtı | Canlı durum | Kalan engel | Sabah gereken karar |
|---|---|---|---|---|---|---|
| 1. OpenAI başvuru paketi | Platformdaki 1.2.0 taslağı (asdk_app_v_6aa205eb…) dolduruldu ve taslak olarak kaydedildi: Info (ad, sürüm 1.2.0, alt başlık, onaylı Description, kategori Productivity, kimlik Business — NivaDesk, yazar EGGCRAFT LIMITED, 4 URL + demo videosu, ikonlar), MCP sunucu URL'si, Testing (giriş bilgileri şifre HARİÇ; 5 pozitif + 3 negatif test; rakamlar canlı handler'larla yeniden doğrulandı), Submit → Release Notes. Global: tüm ülkeler. Yazma testleri koştu: update_order_status fixture'da (bildirim KAPALI, e-posta/telefon yok; aynı statüyle 2. çağrı 2. history satırı ekledi, hiçbir mesaj gitmedi) ve attach_bank_receipt sentetik ESET satırında (OCR ile eşleşti, eski dosya silindi = replace); ESET satırı reviewer için tekrar 'fişsiz' bırakıldı. | deploy dalı: docs/security/evidence/oauth-client-registry-gap-2026-09-10.md, docs/security/evidence/openai-1.2.0-write-tests-2026-09-10.md (bu commit) | write-tests dosyası §1–3; read-only rakamlar aynı dosya §3 | 1.2.0 canlı (değişmedi). Platform: 1.0.0 Published, 1.1.1 Rejected, 1.2.0 Draft (Submit BASILMADI). Not: taslağı 01:20Z'de sürümsüz edit URL'sine gitmem oluşturdu (platform 2.0.0 adıyla boş taslak açtı; 1.2.0'a çevrildi) — 1.1.1 verisi taslağa taşınmaz, hepsi elle girildi | **HIGH (canlı regresyon):** OpenAI/ChatGPT'nin 21 Ağu'da kaydettiği OAuth client'ının kaydı yok (14ff0cfb, 4 Eyl'den beri her connect 'This client is not registered' ile reddediliyor; 4 Eyl'den beri 0 başarılı consent). Bu yüzden Scan Tools (→ tool gerekçe alanları) ve ChatGPT review bağlantısını yeniden kurma BLOCKED; platform 'Client registration is locked' diyor, yeniden kayıt yok. Ayrıca: şifre alanı (benim yazmam yasak), 4 policy onay kutusu + mature-content seçimi (şart kabulü) | (1) chatgptOAuthClients'a TEK kaydı geri yazma kararı (evidence §5; rollback = silme) → sonra Scan Tools + gerekçeler (platform-justifications.json hazır) + review hesabını ChatGPT'de yeniden bağlama; (2) Testing → şifreyi yapıştır; (3) Submit sayfasında policy kutuları + 'No' mature; (4) 16 demo siparişte portalAutoUpdates.enabled=false yapılsın mı (reviewer statü değiştirirse gerçek görünümlü adreslere e-posta gider); (5) Submit'e basılsın mı |
| 2. Web onboarding yayını | Onboarding paketi yayın deposu main'e taşındı (f765d0e cherry-pick, connect değişikliği korundu) ve **Round 169** olarak yayınlandı; ardından checklist 'open_order' derin bağlantısı **Round 170** (4e3a05f, 02:25:00Z canlı, 4 dosya hunk olarak); canlı chunk doğrulaması 02:11:49Z (progress-key/setup=continue/Continue setup). Review hesabı oturumuyla /dashboard ve /home canlıda kontrol edildi (hata yok, sihirbaz yok). Native: open_order eşlemesi eklendi; Android assembleDebug BUILD SUCCESSFUL (2m25s) + iOS xcodebuild BUILD SUCCEEDED bu gece (onboarding worktree); mağaza gönderimi YAPILMADI | yayın deposu `0210a19` + `4e3a05f` (main); kaynak: onboarding-retention (web head b2d1939b); kayıt ship-gate §8 | yerel gerçek next build exit 0 (23 s); canlı chunk sayımı; UI kontrolü oturumda | CANLI Round 169 (Hostinger main). Geri alma: `git revert 0210a19 && git push` | Taze-workspace resume/Skip yolu canlıda doğrulanmadı (giriş gerektiren yeni hesap yok; emülatör kanıtı §2 ile duruyor). onboarding-retention dalı kaynak/deploy dalına TAŞINMADI | (1) onboarding-retention dalının (web+native+functions) deploy dalına --no-ff taşınması; (2) native build'lerin mağaza hattı için arşivlenme zamanı |
| 3. substantiveOrder / v2.1 | Tasarım §3 uygulandı: derive.js kapısı (dalda), getSetupChecklist sınırlı okuma (paymentDate ile, createdAt 450'de 65 siparişte var) + firstOrderProgress, model 'Complete your first project' + open_order hedefi, web href, native eşleme, bileşik indeks (READY). Boş sipariş checklist'i TAMAMLAMIYOR (test + prod aynası). Dry-run: aktivasyon 23→9, tamamı kapıdan; 16 shell-only workspace flip. İlk ölçüm 02:24Z alındı (baseline) | onboarding-retention `eabbe001` + `f295814f` (rollout paketi); deploy dalı `e5695be0` + `b9aeec70` (dar taşıma: derive.js ve web modülü TAŞINMADI) | 6 lifecycle/checklist suite + wiring testi yeşil (iki dalda); deploy dalında tam `npm test` exit 0 (1.489 PASS); prod aynası 3 workspace; web tsc 0 | **getSetupChecklist CANLI** rev `getsetupchecklist-00003-noh` (önceki 00002-zeq); kayıt docs/security/evidence/getsetupchecklist-deploy-2026-09-10.md. Funnel (getActivationFunnel) DEĞİŞMEDİ | Funnel/derive.js cutover yapılmadı (bilinçli): 16 workspace dormant/activated→new olur, mesaj tetikleri etkilenir | activation-v2.1-rollout §3: derive.js + getActivationFunnel by-name deploy'una onay (backfill yok, rollback b9aeec70) |
| 4. Retention (gönderim kapalı) | Eksik wiring `retention-wiring` dalında yazıldı: saf kurallar/şablonlar/yanıt-ayrıştırma/backoff/token (`lifecycle/retention.js`), in-app yazıcı + founder e-posta outbox (retry, dead-letter) + reply ingestion → `user_replied` bastırma + opt-out + dismiss cooldown + (campaign,channel) başına tek log satırı (duplicate) (`retention/writer.js`), messaging.js'e 4 yeni bastırma, index.js'e 5 bayraklı fonksiyon (sweep/inbound/unsubscribe/2 callable). **Hiçbir e-posta/in-app/recovery mesajı gönderilmedi; deploy yok; 4 bayrak unset** | `retention-wiring` @ `091bdc9c` (worktree ~/Developer/studioflow-retention; onboarding-retention f295814f'ten) | retention-rules (13), retention-writer (10, sahte Firestore: bayraklar kapalıyken yazım listesi BOŞ), lifecycle-messaging (+5) yeşil; tam suite sonucu rapora eklenecek | Canlıda hiçbir şey değişmedi | Boşluklar (doc §3): inbound route (provider/IMAP kararı), reply domain, gönderici kimliği, 2 secret, in-app okuyucu istemci, çeviri, firestore.rules deny + outbox indeksi, support-case damgası | (1) inbound yolu: mail provider inbound-parse mi IMAP poller mı; (2) founder gönderici adresi; (3) açma sırası onayı (doc §4: önce SWEEP dry-run) |
| 5. eBay / Etsy hazırlığı | **eBay:** 11 karar (D1–D11) tek tabloda çıkarıldı; rutinler çözüldü: D3 (kodda zaten invoker yok), D5/D8/D10 (runbook sırası), D6 KODDA (6. secret NIVADESK_EBAY_DELETION_VERIFICATION_TOKEN + wiring testi 'six'), D7 (operatör kararı uygulandı), D11 (3 tasarım cümlesi + policy cümlesi). Runtime SA/IAM/Secret Manager/Cloud Tasks teslimatı ve isimle deploy sırası tek runbook'ta (docs/ebay-morning-package-2026-09-10.md §3), ÇALIŞTIRILMADI. Sandbox hazırlık tablosu §4. **Etsy:** panel read-only bakıldı — Chrome'daki Etsy oturumu 'hiç app kayıtlı değil' diyor (doğru hesap değil; onaylı `nivadesk` app EGGcraft hesabında, 31 Ağu kaydı) → durum BELİRSİZ; runtime envanteri + AC-ETSY matrisi + Mac etsySource fix hazırlığı ayrı dosyada | ebay-connector `5a3ea7f6` (worktree ~/Developer/studioflow-ebay); Etsy: deploy dalı docs commit (aşağıda) | eBay: 13 qa suite yeşil + 2 emülatör e2e (connector + account-deletion) `E2E_EXIT 0`; Etsy: qa suite sonuçları Etsy dosyasında | GCP'de hiçbir şey değişmedi: SA yok, secret yok, eBay servisi yok; Etsy'nin 11 fonksiyonu zaten canlı (envanter) | eBay: 4 IAM onayı (D1, D2, D4, D9) + secret değerleri + marker + deploy; Etsy: doğru hesapla panel kontrolü | (1) D1/D2/D4/D9 onayı → runbook §3; (2) Etsy your-apps'i EGGcraft hesabıyla aç; (3) Mac etsySource fix'ini mağaza hattına almak |
| 6. Açık takipler | **Google 75151719:** Cloud Console'daki oturum `gunes.gocmen@gmail.com` (proje seçili değil) → vaka sayfası boş; yeni yanıt OKUNAMADI, durum 'test sonuçları iletildi; yanıt bekleniyor' olarak kalıyor (tekrar denenmedi). **Stripe:** 9 Eyl 22:00Z'den beri `stripewebhook`'a 0 POST, 0 hata; `resyncStripeWorkspaceEntitlements` 0 çağrı → trafik yok = davranış gözlenmedi; 24 saat penceresi dolmadı. **İzleme:** proje şu an 0 alarm politikası / 0 bildirim kanalı; 4 sinyallik dar öneri yazıldı (OAuth refusal, consent sessizliği, Stripe webhook hatası, 5xx tabanı) — hiçbiri oluşturulmadı. **WhatsApp W2:** kapalı; model sınırı fail-closed, provider DPA/noTraining kanıt alanları boş (insan dolduracak) — dokunulmadı | deploy dalı docs: docs/security/monitoring-proposal-2026-09-10.md, docs/etsy-runtime-inventory-2026-09-10.md | log okumaları (read-only) | Canlıda değişiklik yok | Google yanıtı doğru hesapla okunmalı; Stripe 24h sonrası tekrar kontrol | (1) izleme: alıcı contact@eggcraft.co.uk + M4 eşiği → sonra komutlar; (2) Google vakasını contact@ hesabıyla aç |

## Zaman çizelgesi
- 01:20Z — rapor açıldı; 1. iş başladı.
- 01:36Z — Scan Tools → OAuth: chatgptOAuthAuthorize 'unregistered_client' (client 21 Ağu kayıtlı, kaydı yok; 14ff0cfb). HIGH kaydedildi, üretime yazılmadı.
- 01:55Z — update_order_status yazma testi (fixture pMfJ9be6wE1ZC1wzmns1), bildirim yok. 01:59Z — attach_bank_receipt ESET (demo) eşleşti/replace; sonra fişsiz bırakıldı.
- 02:05Z — taslak formu tamamlandı (scan + gerekçeler + şifre + policy hariç); Submit basılmadı.
- 02:08Z — Round 169 push (0210a19); 02:11:49Z canlı (chunk). Review oturumuyla UI kontrolü tamam.
- 02:25Z — Round 170 canlı (open_order href). 02:29Z — getSetupChecklist deploy (00003-noh); indeks READY. v2.1 dry-run 23→9, rollout paketi yazıldı, cutover yok.
- 02:45Z — retention wiring commit 091bdc9c (bayraklar kapalı, deploy yok).
- 02:52Z — eBay paketi commit 5a3ea7f6 (D6 kod, D11 doc, runbook); eBay e2e emülatör yeşil. Etsy paneli: oturumdaki hesapta app yok → belirsiz.
- 03:02Z — Etsy envanter + AC-ETSY matrisi; izleme önerisi; Stripe/Google/W2 kayıtları. Deploy dalı docs push.
