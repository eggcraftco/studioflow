# NivaDesk — current hand-off record (written 10 September 2026, 19:10Z; last updated 11 September 2026, 13:16Z)

Read this first after a context reset or in a new session, then compare it with the live git and cloud state before
acting. Do not repeat work listed as done; do not treat candidates in older summaries as the current state. No password,
token, OAuth code or secret value appears here.

## 0. Repository state at hand-off

| | |
|---|---|
| Deploy branch | `macbook-save-before-macstudio-2026-06-01` — this is the branch functions are deployed from, by name only |
| HEAD | **`ff514cf8`** (merge of `funnel-store-connections`, 11 Sep 11:3xZ — source of the live `getactivationfunnel-00004-tot`; on top of `fd2a6648`, source of `submitfeedback-00003-jez`) plus the record/hand-off commits; == origin; working tree clean |
| **Credentials on this Mac** | renewed by the operator 11 Sep ~11:15Z (`gcloud auth login`, ADC, `firebase login --reauth`); verified read-only: account contact@eggcraft.co.uk (`roles/owner`), project eggcraft-studio, ADC Firestore read OK, Firebase CLI lists the project. Secret metadata: `NIVADESK_SMTP_PASSWORD` exists (28 Jun); `NIVADESK_RETENTION_TOKEN_SECRET` created 11 Sep 12:44:45Z (v1); `_INBOUND_SECRET` does not exist. All three expire together (`invalid_rapt`, seen 11:00Z): check `gcloud auth list` + a small ADC read before any deploy. |
| Deploy runbook | `docs/audit-deploy-checklist.md` — ancestor check for the Stripe fix `76c5e3c3`, clean tree, `functions/.env` present, `functions/` == origin tip, deploy by name never `--only functions`; **new section:** the shared `.env` carries `NIVADESK_EBAY_CONNECTOR=1` and rides every later eBay deploy — re-check scope before each |
| Local-only deploy prerequisites (not in git) | `functions/.env` (ignored; holds the sandbox lines `NIVADESK_EBAY_ENVIRONMENT=sandbox`, `NIVADESK_EBAY_RUNAME=EGGCRAFT_LIMITE-EGGCRAFT-NivaDe-nerasfwi`, `NIVADESK_EBAY_CONNECTOR=1`) and the marker `functions/.ebay-secrets-ready` (ignored since `e7f59525`); both exist on this machine |
| Side branches, all pushed | **11 Sep candidates, not merged:** `retention-email-candidate` @ `3d02ef73` (code `3670c58e`) — not merged. **`ebay-availability-fix` @ `333e20ae` (code `8f63e803`, worktree `~/Developer/studioflow-ebay-availability`, CI green) — not merged, not deployed:** `getEbayConnections.workspaceEnabled` + all clients hide Connect when false; five-platform record `docs/onboarding/ebay-availability-fix-2026-09-11.md` (§3 matrix, §4 defects found on the way, §5 what is missing: physical phones, store builds, member role on phones). Deploy = only `getEbayConnections` from that branch, then the store package. **Merged 11 Sep:** `native-feedback-screens` (`fd2a6648`) and `funnel-store-connections` (`ff514cf8`); their worktrees `~/Developer/studioflow-native-feedback`, `~/Developer/studioflow-funnel` can be removed. `ebay-workspace-allowlist` @ `120615ad` — already merged into the deploy branch (`def97f49`), kept for history. Not merged: `activation-v21-cutover` @ `3f20e04f` (branch only, no worktree), `retention-wiring` @ `55f6fa4d`, `onboarding-retention` @ `4d525de4` (the older source of the activation work) |
| Worktrees (`git worktree list`) | `~/Developer/studioflow-retention` (`retention-wiring`), `~/Developer/studioflow-onboarding` (`onboarding-retention`), `~/Developer/studioflow-stripe` (`stripe-trial-stamp`, already merged), `~/Developer/studioflow-openai-review` (`openai-resubmission`), `~/Developer/studioflow-ebay` and `studioflow-ebay-carry` (superseded eBay candidates), `studioflow-mcp`, `studioflow-ssrf`, `studioflow-whatsapp`, `studioflow-hostinger-sync-20260530`; the web publish repo `~/Developer/studioflow-hostinger-publish-20260530` is separate (Round 170 `4e3a05f` live). Functions are deployed only from the main checkout |
| Background jobs | none running; no pending deploy; no scheduled watches |
| Credentials | gcloud user + ADC + Firebase CLI were re-authenticated by the operator between 11:13Z and 11:21Z on 10 Sep after a session-reauth wall broke a deploy mid-run (rollout doc §5); check `gcloud auth print-access-token` before any long deploy |
| Chrome (claude-in-chrome tab group) | tab "OpenAI Platform" (platform.openai.com/plugins) and tab "NivaDesk" signed in as `contact@nivadesk.co.uk` on Settings → Integrations → eBay; the Chrome profile's nivadesk.app session is now this account (it was `review@nivadesk.app` before — never act in that session) |

## 1. eBay — the last verified state (the live work of today)

| Item | State |
|---|---|
| Functions | all 17 eBay functions live on `ebay-connector@eggcraft-studio.iam.gserviceaccount.com` with the six secrets at version 1. **Four carry the connector switch** (`NIVADESK_EBAY_CONNECTOR=1`, revision `-00002-`): `beginEbayConnect`, `claimEbayConnectState`, `ebayOAuthCallback`, `getEbayConnections`. **The other twelve and the worker are switched off** (revision `-00001-`, no env): `syncEbayNow`, `previewEbayImport`, `runEbayImport`, `retryEbayImportFailures`, `verifyEbayConnection`, `updateEbayConnectionSettings`, `disconnectEbay`, `revealRestrictedCustomer`, `reconcileEbayConnections`, `reconcileEbayConnectionsNightly`, `reconcileEbayDeletions`, `ebayNotifications`, `ebayEventWorker` |
| Workspace gate (code) | `beginEbayConnect`, the native claim and the callback require an explicit `appConfig/commerce` → `connectors.workspaces["ebay:<companyId>"]: true` (or `"ebay:*"`); the provider switch alone opens nothing; a workspace delisted mid-flow is refused at the callback (`reason=workspace`, state burned, code spent) — merged `def97f49` |
| Flag document | `appConfig/commerce.connectors` = `{ providers: { ebay: true }, workspaces: { "ebay:GuglEFKSEKNTq1xibFpJav3EWkY2": true } }` — **only this workspace may begin a connection**; `shadow` and `queue` untouched |
| NivaDesk test account | **`contact@nivadesk.co.uk`**, Auth uid `GuglEFKSEKNTq1xibFpJav3EWkY2` — do not confuse with `contact@eggcraft.co.uk` (the operator's Google/GCP identity and the real EGGcraft workspace) or `review@nivadesk.app` (OpenAI review, off-limits) |
| Test workspace | **`GuglEFKSEKNTq1xibFpJav3EWkY2`, name "test"**, owner = the account above (verified), 0 orders, 0 customers, plan trialing; the app allows one workspace per account |
| Sandbox seller | **`TESTUSER_nivadesk_seller1`** (eBay sandbox user registered on 10 Sep before the 18:53Z OAuth begin — form filled by the assistant with non-secret values, password and *Register* by the operator; seller id `mtm4ubrcsv2`, site EBAY_GB); password known only to the operator |
| Connection | **`ebayConnections/GuglEFKSEKNTq1xibFpJav3EWkY2__mtm4ubrcsv2`**: `status: connected`, `environment: sandbox`, `readOnly: true`, scopes `sell.fulfillment.readonly` + `commerce.identity.readonly`, marketplace EBAY_GB/GBP, `importState: none`, `lastSyncAtMs: 0`; credentials in `credentials/current` (encrypted, never read) |
| Verified | OAuth completed 19:01:46–19:01:51Z; read-only *Check now* (`verifyEbayConnection` → one `getOrders` limit 1) HTTP 200 at 19:03:07Z; `ebayQuota/2026-09-10` calls 1. Evidence commit **`447ef785`** (`docs/ebay-sandbox-opening-2026-09-10.md` §8) |
| `autoSync` | `true` on the connection, **but the sweeps run with the switch off**, so nothing syncs. Deploying `reconcileEbayConnections`/`reconcileEbayConnectionsNightly`/`ebayEventWorker` with the shared `.env` **will start automatic syncing of this connection** — decide before that deploy |
| Presentation fix (11 Sep 16:0xZ) | eBay was shown as **Available** with Connect to every workspace while the server refused begin. Fixed on `ebay-availability-fix` (`8f63e803`): `getEbayConnections` returns `workspaceEnabled` (same four gates as `beginEbayConnect`); web/Mac/iPhone/Android show "eBay is not available for this workspace yet." / Coming soon when false; old clients ignore the field. Verified per platform on emulators/simulator/local Mac (record above); **not deployed, not in any store build**. |
| Manual stage (**eBay replied on 260910-000068 — received 11 Sep 01:32Z, evaluated**) | `docs/ebay-manual-stage-2026-09-10.md` §1–§2m. eBay Developer Support: Sandbox order creation "is not operational", engineering working, no ETA; recommends production testing under the Test Listings Policy. This explains the empty Fulfillment API (not propagation, not a NivaDesk filter). Our Trading-side order stays `Completed`/checkout `Complete`, £5.00 + £1.00 = £6.00; import acceptance tests **still not completed**; `autoSync` off; nothing sent, no new test. Reply draft in §2m (not sent) **§2n (11 Sep 11:0xZ): the reply verbatim; eBay's words = Sandbox order creation not operational, no date, 'if applicable' test in production under the Test Listings Policy; the production keyset/RuName/buyer mechanics are OUR inference (§2m option 2), not eBay's. Q1 (will the existing order surface, and when) OPEN, unaddressed; Q2 (supported Sandbox path) answered indirectly: none today.** | **morning decision:** (1) wait for the Sandbox fix and send the §2m reply asking for notification + whether the existing order will surface — *recommended*; (2) a production test under the Test Listings Policy (production keyset/RuName, real seller + buyer, fees) — scope in §2m; (3) leave blocked |
| Not done / not approved | payment, refund, shipping, automatic sync tests. The manual stage was **prepared** in `docs/ebay-manual-sync-stage-prep-2026-09-10.md` (19:40–20:05Z, read-only): deploy list = `previewEbayImport`, `runEbayImport`, `syncEbayNow` (optional `retryEbayImportFailures`) from `c5dfee5a`; worker not needed (plan `team_monthly` has no order limit); `autoSync` off via the web checkbox (ungated callable, no deploy); no flag change needed; no sandbox order exists yet — one must be created (buyer test user + AutoPay-false listing + purchase); acceptance table and rollback in that file. Live state re-checked against this record: no differences; Scheduler jobs enabled with switched-off targets; 2 web push tokens on the test workspace |
| Separate gates | production keyset (not created) → portal Marketplace Account Deletion registration (endpoint proven with the token: challenge hash matched 12:56Z); production OAuth/RuName/Worker; the design's test B (impersonated enqueue) — superseded by the synthetic-ledger proof |
| Proven earlier today | runtime SA enqueue leg (synthetic ledger row 13:45Z, deleted after), TTL policies on `ebayConnectStates` / `ebayPresentedCodes` / `ebayDeletionRequests` ACTIVE, Firestore rules ruleset `8256326f` == committed file, Hostinger `NIVADESK_EBAY_CALLBACK_KEY` == secret (minted ticket sealed 204), delivery tests A2/A2-compute |
| Evidence files | `docs/ebay-sandbox-rollout-2026-09-10.md` (step 1: SA, secrets, worker, delivery tests), `docs/ebay-sandbox-backend-2026-09-10.md` (step 2: sixteen functions, rules), `docs/ebay-relay-key-and-deletion-token-2026-09-10.md`, `docs/ebay-autonomous-2026-09-10.md` (enqueue proof, TTL, allowlist), `docs/ebay-sandbox-opening-2026-09-10.md` (gate live, connection), `docs/ebay-operator-handoff-2026-09-10.md`; raw folders beside them with SHA-256 manifests |
| Rollback pointers | rollout §7 (step 1 resources), backend §6 (the sixteen); the opening doc has no rollback section — to undo today's opening: remove the workspace entry from `appConfig/commerce` (closes begin, claim and callback at once), delete the connection document (`disconnectEbay` is switched off and would refuse), and only if the gate itself must go `git revert -m 1 def97f49` plus a redeploy of the four by name |

## 2. Other live threads

| Thread | Verified state | Next |
|---|---|---|
| OpenAI ChatGPT app 1.2.0 | **In Review** — submitted by the operator 10 Sep 09:12Z; the platform's version list showed 1.2.0 Review, 1.1.1 Rejected, 1.0.0 Published, and still Review in the open Chrome tab at hand-off; **re-read 11 Sep 02:57Z (read-only, platform.openai.com/plugins): 1.2.0 Review, 1.1.1 Rejected, 1.0.0 Published — unchanged, no message to answer.** MCP/OAuth surface frozen: no redeploy of `chatgptMcp`, `chatgptOAuthAuthorize` or the other OAuth functions (revisions `chatgptmcp-00073-fuz`, `chatgptoauthauthorize-00045-has` unchanged); the review workspace `KSQidetb3oOSItE9amLISf9Lh6h2` / `review@nivadesk.app` untouched | wait for OpenAI; after a decision see memory `openai-review-next-steps` |
| Stripe L1 / trial-stamp fix | `76c5e3c3` live (`stripewebhook-00047-por`, `resyncstripeworkspaceentitlements-00032-xej`) and on the deploy branch (`b6b30acc`); **natural-traffic observation: no alert policy exists and nobody is watching it automatically** — the last verified statement is the 24-hour check window noted in memory (until 10 Sep 23:00:56Z); read the Stripe evidence `docs/security/evidence/stripe-l1-deploy-2026-09-10.md` before claiming anything newer | a deliberate log check after the window, not a claim |
| Google support case 75151719 (Amazon CRTD) | the corrected test ran once (job `crtd-test-base64-elf-2026-09-10-09-28-57-utc`), no finding; the reply was sent by the operator from the e-mail thread 10 Sep ~11:50Z; **awaiting the Product Specialist**; Amazon Control 3 stays open; Developer Profile not submitted; no new test or message. **Read-only check attempted 11 Sep 02:57Z, NOT possible:** the console support page did not render in the Chrome profile (its scripts from gstatic failed to load; an error card "There is an issue with the service you are attempting to reach"), and the Cloud Support API is disabled on `nivadesk-amazon` (403 on a GET; enabling it is a project change and was not done). The case's e-mail thread was not opened (no mailbox access). **Update relayed by the operator 11 Sep 11:00Z (from the case's e-mail thread, not read by this session):** Murali forwarded the latest evidence to the Product Specialist team and committed to a status update by **15 Sep 2026 22:30 IST / 17:00 UTC**. Case not resolved; Control 3 open; no new message or test needed | wait |
| Activation v2.1 cutover | **LIVE 11 Sep 01:15Z**: candidate `activation-v21-cutover-candidate` (code `21d13bb4`, CI green) merged into the deploy branch as **`088c673e`** (functions/ tree identical to the tested candidate); only `getActivationFunnel` deployed → **`getactivationfunnel-00003-cuv`** (100 % traffic; previous `-00002-ret` of 6 Sep retained, `-00001-muv` too). Live admin funnel 01:16Z: **66 workspaces, 11 activated**, stages 34/18/3/1/2/6/2 — identical to the same-minute read-only dry run (OLD would have said 25). Scope unchanged: the funnel counts every company, so the test/internal workspaces (`FvnnEcQA…` "test", `GuglEFKS…`, EGGcraft, `KSQidetb…`) are in its totals. Only reader of `derive.js` is this function and it writes nothing; checklist, feedback (live, general availability) and retention (flags unset) untouched. Evidence `docs/onboarding/activation-v2.1-cutover-2026-09-10.md` §4–§5 | **rollback = route traffic back to `getactivationfunnel-00002-ret`** (its own image and env); a source redeploy from `8bd71b57` would carry today's shared `.env` and is not a full return by itself. Next: decide separately whether the admin funnel should exclude test/internal workspaces. **Funnel store-connections candidate, NOT deployed:** worktree `~/Developer/studioflow-funnel`, branch `funnel-store-connections`, code `ea0ce5d0`, record `90bab7a2` = `docs/onboarding/activation-funnel-stores-2026-09-11.md` (the snapshot carries the five store collections; dry run 11 Sep: 66 workspaces, `integration_connected` 0→3, activated 10→10, 0 state changes; CI functions-tests success). **Second pass 11 Sep 11:12Z (code `22dedd67`, record `b92c464c`):** derive.js judges each connector by its own status words (Shopify `active` only — an uninstalled store no longer counts, the live reader counts it; Etsy/Woo/Square/eBay `connected`/`reconnect_required`/`needs_reconnect`), 8 scenario checks in the writers' shapes. **The dry run must be re-run after the credential renewal before any deploy.** Scope: `getActivationFunnel` only; rollback `getactivationfunnel-00003-cuv`. **Third pass 11:2xZ (`988e740c`):** readers of derive.js pinned (funnel + `nvRetentionTriggerFor` → `retentionSweep`/`getRetentionMessage`; checklist is not one); consumer test shows the rule difference per reader (funnel stage one step back, `connect_first_store` proposed, pending card not withdrawn) — only for an uninstalled/pending/unlinked Shopify row; **fresh live-vs-candidate dry run: 66 workspaces, 0 differ, no such row exists today**. Exact scope: `getActivationFunnel` only; retention functions not made mandatory. **DEPLOYED 11 Sep 11:41:35Z → `getactivationfunnel-00004-tot` (100 %)** from the deploy-branch merge `ff514cf8` (funnel files == `988e740c`); live admin read at ~11:43Z = same-minute dry run: 66 workspaces / activated 10 / customers 10 / orders 28, stages 34/18/4/1/1/6/2 (activated 11→10 since 01:16Z = the pilot workspace's synthetic order in the bin, not the rule). **Rollback: traffic to `getactivationfunnel-00003-cuv`** (retained, Ready). Retention readers redeployed 12:10Z (see the retention row) — the rule difference is **closed** |
| **Feedback v1 (onboarding feedback)** | **LIVE FOR EVERY WORKSPACE, general-release check complete** (10 Sep): `NIVADESK_FEEDBACK_WORKSPACES=*`, invite window from **23:39:26Z** (`1789083566000`), six callables at `-00002-*`, web Round 172 `1c4eaed`. Manual **Send feedback** for everyone; the automatic invitation only for a first substantive order created after 23:39:26Z (older successes never invite: tests, local verdict, live). Last verification 23:54–23:58Z in EGGcraft (outside the pilot before): entry visible, one QA send (`fb_HLGCNn1OHZw-`) reached the inbox with the right account/workspace and was closed as a test; no notification/mail/order side effects. Inbox = three admin addresses; no e-mail/push. **Rollback:** restore the two previous `.env` values and redeploy the six by name; **feedback records are never deleted by a rollback.** Evidence `docs/onboarding/feedback-pilot-package-2026-09-10.md` §11b–11h. **Native screens: candidate branch `native-feedback-screens` (worktree `~/Developer/studioflow-native-feedback`, code `c0deb2ba` + record), NOT released** — manual "Send feedback" entry in the Mac/iPad avatar menu, the iPhone menu and both Android header menus when `getFeedbackPrompt.enabled` (10-minute cache), the web's form/thank-you on the same callables, 22 keys × 11 languages in both native dictionaries; macOS + iOS simulator BUILD SUCCEEDED, Android compileDebugKotlin + assembleDebug BUILD SUCCESSFUL (debug APK not uploaded); no invitation card; server candidate: `submitFeedback` accepts `platform` web|mac|ios|android (test added) — until deployed, native notes are filed as `web`. **Emulator end-to-end 11 Sep 11:04Z:** mac/android/web-shaped sends → records with the right workspace and `platform` (web-shaped = `web`, old client unchanged) → admin inbox rows + detail; `platform: windows` refused 400; non-admin refused. **DEPLOYED 11 Sep 11:27:57Z: `submitFeedback` only → `submitfeedback-00003-jez` (100 %), from the deploy-branch merge `fd2a6648` (functions/ == tested `c0deb2ba`; CI success); rollback = traffic to `submitfeedback-00002-jih`; env = previous names + the shared `.env`'s retention lines (NIVADESK_RETENTION_EXCLUDE_WORKSPACES, NIVADESK_RETENTION_IN_APP, NIVADESK_RETENTION_SWEEP, NIVADESK_RETENTION_WORKSPACES), not read by this function; nothing else deployed, no store build (§3c).** **Real-device Mac check DONE 11 Sep 12:02Z (§3d):** signed Debug build from `ff514cf8`, account `contact@nivadesk.co.uk` / workspace `test` verified in-app and by a read; one marked note → `fb_uFGXmTi0BxcX` (`source in_app`, `platform mac`, `trigger manual`, `page mac/home`); admin inbox shows it with `mac`, closed with a test note at 12:04:48Z, record kept; no mail/push, business data unchanged. iOS/Android: build + emulator evidence only, no real-device send — **Store packages, one per platform: `docs/onboarding/native-store-package-2026-09-11.md` (13:16Z)** — real product diff (iOS/Mac 231 non-merge app commits since 1.3 (17) `ad79d3da`; Android 210 since 0.1.8 `0563fc9c`, 61 since the 0.1.9 AAB of 31 Aug whose upload is unconfirmed), every newly called callable live (none missing), **finding: eBay is presented as available to every workspace while `beginEbayConnect` is gated per workspace (recommended small server+client change before the build)**, evidence table (Android unit suite 33 green + Swift finance mirror 36 on the candidate; runtime flows need the operator's session on a non-pilot workspace), versions (Apple 1.4 (18) recommended; Android 0.1.9 (10) vs 0.2.0 (11) depends on Play), signing (only Apple Development identity here — Distribution needs the operator's Xcode sign-in; Android upload keystore present), store records not readable (ASC not signed in; Chrome Google account has no Play developer account). Old store builds prepared for an upgrade-over-install test. Record `docs/onboarding/native-feedback-screens-2026-09-11.md` (`23803da8`). **Still open:** other invitation types | watch the first real submissions in the inbox; morning: one manual send from the Mac app (test workspace), decide the `platform` deploy, then the store cadence |
| Retention wiring | **IN-APP PILOT LIVE on the server (11 Sep 02:20Z), one workspace `GuglEFKSEKNTq1xibFpJav3EWkY2`**: deploy branch merge `9aacbe5b` (functions tree = CI-tested `dd9fc6e9`); rules + the `retentionLog` index released; flags `SWEEP=1`, `IN_APP=1`, `WORKSPACES=<pilot>`, `EXCLUDE=<3 ids>`, `EMAIL`/`INBOUND` unset; eight functions by name — **12:10Z: `retentionSweep` → `retentionsweep-00002-maz` and `getRetentionMessage` → `getretentionmessage-00002-woz` redeployed from `ff514cf8`+ for the per-connector Shopify rule (retention modules unchanged; flags read back identical; scheduler untouched, not triggered; rollback `-00001-cob` / `-00001-yoy`; §9)** — original new `retentionsweep-00001-cob`, `getretentionmessage-00001-yoy`, `dismissretentionmessage-00001-jej`, `setretentionoptout-00001-jat`; updated `createsupportticket-00068-lot`, `createworkspaceticket-00049-dux`, `updatesupportticketstatus-00037-dup`, `updateworkspaceticketstatus-00041-feb`; scheduler hourly ENABLED. Web publish repo **Round 173** `3edb0b0`, live 02:23:44Z; EGGcraft session shows no card (excluded) and keeps Send feedback. First sweep (by hand, 02:20:59Z): evaluated 1, skipped not_in_pilot 62 / excluded 3, sent 0, the only candidate (`founder_intro`, e-mail) refused `flag_off`. The synthetic order `YFFB4Xqi8zSfFgPEN48t` is **in the bin, marked** (`retentionPilotNote`) so a first-project card can become due; **first possible card moved to 12 Sep ≈12:20Z** — the Mac QA feedback send at 11 Sep 12:02:30.875Z counts as a `submissions` stamp and the 24-hour hold reads shows + submissions (not dismissals); `create_first_order` itself is due from 11 Sep 18:31:18Z (§10). The 11 Sep 23:20Z figure is void. Evidence `docs/onboarding/retention-wiring-2026-09-10.md` §7–§8. **Pilot follow-up pending — no persistent watcher exists in the session:** the first natural card is now expected at the **12 Sep ≈12:20Z** sweep (§10), then the restore of the synthetic order and the `goal_met` withdrawal; natural sweeps so far read `evaluated 1`, `sent 0`. **E-mail stage: deploy-ready candidate, NOT deployed** — branch `retention-email-candidate` (from `37025406`), code **`3670c58e`**, record `3d02ef73` = `docs/onboarding/retention-email-candidate-2026-09-11.md` (cherry-picked here): reply key in the subject tag, the note's Message-ID kept beside the key (reply matched by `In-Reply-To` when the tag is lost), one inbound record per Message-ID, the outbox recheck (opt-out/reply/support case/cancellation/activation for setup mails), the sending mailbox as reply address, and a read-only hourly IMAP poller `retentionInboundPoll` (new function) behind `INBOUND=1` + `INBOUND_MODE=imap` with `imapflow ^1.7.8`; tests rules 17 / writer 18 / wiring 7 / poller 4, full `npm test` exit 0; CI functions-tests was in progress at 03:0xZ (dependency-audit success). §5 rewritten 11 Sep 11:1xZ (`d2b785aa`): Hostinger IMAP `imap.hostinger.com:993` / SMTP `smtp.hostinger.com:465` verified from the documentation, `.env` has no SMTP line (code defaults apply), routine choices made with reasons (hourly poll, founder note not stopped by activation, imap mode), the token secret is an operation the assistant runs after the CLI sign-in (`firebase functions:secrets:set`, Owner/Secret Manager Admin). **Decisions 1–3 applied 12:47Z (§7, code `4018160f`, record `ca530686`):** identity contact@nivadesk.co.uk approved; `NIVADESK_RETENTION_TOKEN_SECRET` CREATED 12:44:45Z (v1; value never printed; bound in code to `retentionSweep` + `retentionUnsubscribe` only); same-mailbox reply contract (key + expected sender + In-Reply-To/References; the own note is never a reply; refused mails write nothing; poller judges by headers, opens only proven answers); one read-only IMAP connection check OK (imap.hostinger.com:993, INBOX 48 messages, 0 tagged, nothing touched). Credential scope unchanged (mailbox password; the limit is in the code). **E-mail preparation COMPLETE (operator, 11 Sep ~12:50Z); EMAIL/INBOUND unset, poller not deployed. Next, in order: (1) 12 Sep ≈12:20Z natural card visible → (2) synthetic order restored to its recorded state → (3) pending card withdrawn `goal_met`; then re-evaluate e-mail eligibility and bring the package for one test e-mail with TWO separate acceptance steps: the first note leaves, and a real reply is processed. Until (1)–(3): no feedback from the pilot account on web/Mac/iOS/Android, no stamp/rule change, no persistent watcher (no automatic follow-up is claimed).** | **Pilot live check — three SEPARATE pieces of evidence, not one chain on one card** (rules: a closed card is never re-shown; the same campaign is `already_sent` for good; `goal_met` withdraws only an OPEN card; dismissal starts a 30-day cooldown for new cards). (A) after the **12 Sep ≈12:20Z** sweep (the first with the feedback hold expired; earlier runs will log `feedback_prompt_recent`): summary `evaluated 1`, one `retentionMessages` row `status: open`, the card visible in the pilot workspace. (B) on THAT card choose ONE: either `goal_met` — **restore the synthetic order** (`siparisler/YFFB4Xqi8zSfFgPEN48t`, clear `isDeleted`; the order is STILL IN THE BIN) while the card is open, then read it again → `status: withdrawn, reason goal_met` — *recommended, it is the check the pilot was set up for*; or Not now → `dismissed` + cooldown. (C) `acted` (the CTA) and Not now need cards of their own: later campaigns (`finish_onboarding`, `connect_first_store`, `connect_bank`, at most two a day) or a fresh synthetic first-order state; record each on its own card. After (B) the order must be restored either way (nothing deleted). **Rollback:** unset the four `.env` lines and redeploy the eight names (or pause `firebase-schedule-retentionSweep-europe-west2`); ticket callables → traffic back to `-00067-juw / -00048-ruk / -00036-loj / -00040-rip`; nothing deleted |
| Etsy | runtime == code (11 functions, deployed 6 Sep, no Etsy source change since); Android order path safe (`updateWebOrder` → field-level `transaction.update`); store builds still pre-2 Sep merge fix; developer-account verification needs the shop's own login (the Chrome Etsy session is a personal profile with no apps) — **user session needed**; no submission, e-mail or store publish | wait for the operator |
| Gmail filter | done 11:36Z in contact@eggcraft.co.uk: `from:(notifications@github.com) subject:"Run failed:" subject:"eggcraftco/studioflow-app"` → skip inbox, mark read, label `NivaDesk/GitHub CI`; 26 existing threads relabelled — **do not redo** | — |
| CI | `functions-tests.yml` parses again since `658fe5f8` (the relay job name was an unquoted colon; every push had produced "No jobs were run" since 4e74929f); runs green on the deploy branch and the three side branches; docs-only pushes no longer trigger it | — |
| Amazon hardened project | `nivadesk-amazon`: CRTD five tests no finding; ETD closed by Google as tier limitation; nothing else pending beyond the case reply | — |

## 3. Standing rules still in force

- Deploy only by name from the deploy branch after the runbook checks; never `--only functions`; never from another worktree.
- Preserve the Stripe fix in every merge; do not redeploy OpenAI MCP/OAuth, Stripe or checklist functions.
- **Standing test scope (11 Sep 2026):** a change that touches a user flow is evaluated on five platforms, each on its own line — macOS app, iOS app, Android app, iPhone Safari, Android Chrome — with the evidence type named (build / simulator-emulator flow / physical device / unit test / distribution package are not interchangeable), the unaffected platforms listed with a reason, and phone checks covering sign-in/workspace, navigation, form fill-and-save, keyboard-open usability and data surviving a reopen. Checklist: `docs/release-checklist-platforms.md`.
- No secret, token, password, OAuth code or PKCE verifier in logs, evidence, chat or shell history; the assistant never types
  credentials into third-party forms (hPanel, eBay portal, Etsy, Gmail) — the operator does.
- No production eBay keyset/OAuth; no portal deletion-token registration without the production-keyset decision.
- Ownership/membership of workspaces is never changed from the database; no plan purchases.
- Blocked actions are recorded as "awaiting approval", "user session needed" or "automatic security check refused" —
  never retried through another tool or identity.

## 4. How to resume

1. `git fetch && git status` on the deploy branch; confirm HEAD `447ef785` or later and a clean tree; `gcloud auth print-access-token` works.
2. Read `docs/night-report-2026-09-10.md` (the single timeline) and the eBay evidence files above; compare with
   `gcloud run services describe <name>` revisions for the seventeen and with `appConfig/commerce`.
3. Take the operator's next instruction; the first pending decision is the eBay manual stage prepared in
   `docs/ebay-manual-sync-stage-prep-2026-09-10.md` (three functions by name, `autoSync` off first, one real sandbox order).
