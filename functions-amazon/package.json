{
  "name": "nivadesk-amazon",
  "version": "0.1.0",
  "private": true,
  "description": "NivaDesk's Amazon connector, deployed alone in the nivadesk-amazon project. Holds Amazon Information; emits only schema-allowlisted sanitized envelopes.",
  "engines": {
    "node": "22"
  },
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "for f in test/*.test.js; do node \"$f\" || exit 1; done",
    "check": "for f in src/*.js src/amazon/*.js; do node --check \"$f\" || exit 1; done"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^5.6.0",
    "firebase-admin": "^13.4.0",
    "google-auth-library": "^9.15.1"
  }
}
