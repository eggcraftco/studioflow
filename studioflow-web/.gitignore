node_modules
.next
.next-*
.next-corrupt-cache*
out
.env.local
.env*.local
.DS_Store
tsconfig.tsbuildinfo
# npm run test:relay compiles the callback route into a temp dir here (it has to
# live under the web tree or `next` does not resolve); it is removed on exit.
.ebay-relay-check-*

# Local demo videos — never publish (3.4GB; GitHub rejects >100MB anyway)
public/NivaDesk_Demo_high.mov
public/NivaDesk_Demo.mp4
